<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup — MediaServer</title>
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        :root {
            --surface:  #1a1a1a;
            --surface2: #242424;
            --border:   #333;
            --text:     #e0e0e0;
            --muted:    #888;
            --accent:   #5b9cf6;
        }
        body {
            background: #111;
            color: var(--text);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 2.5rem 2rem;
            width: 100%;
            max-width: 380px;
        }
        .brand {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--accent);
            letter-spacing: -0.3px;
            margin-bottom: 0.5rem;
            display: block;
        }
        .subtitle {
            font-size: 0.82rem;
            color: var(--muted);
            margin-bottom: 2rem;
            line-height: 1.5;
        }
        h1 { font-size: 1.1rem; font-weight: 600; margin-bottom: 0.3rem; }
        .field { margin-bottom: 1rem; }
        .field label {
            display: block;
            font-size: 0.8rem;
            color: var(--muted);
            margin-bottom: 0.35rem;
        }
        .field input {
            width: 100%;
            background: var(--surface2);
            border: 1px solid var(--border);
            border-radius: 6px;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            font-size: 0.9rem;
            font-family: inherit;
            transition: border-color 0.15s;
        }
        .field input:focus { outline: none; border-color: var(--accent); }
        .error {
            background: rgba(239, 68, 68, 0.12);
            border: 1px solid rgba(239, 68, 68, 0.35);
            border-radius: 6px;
            color: #f87171;
            font-size: 0.83rem;
            padding: 0.6rem 0.85rem;
            margin-bottom: 1.25rem;
        }
        .hint { font-size: 0.75rem; color: var(--muted); margin-top: 0.3rem; }
        .btn {
            width: 100%;
            background: var(--accent);
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 0.6rem;
            font-size: 0.9rem;
            font-weight: 500;
            cursor: pointer;
            margin-top: 0.5rem;
            transition: opacity 0.15s;
        }
        .btn:hover { opacity: 0.88; }
    </style>
</head>
<body>
    <div class="card">
        <span class="brand">MediaServer</span>
        <h1>Create admin account</h1>
        <p class="subtitle">Welcome! Set up your admin account to get started. Additional users can be added from the Users page.</p>

        {% if error %}
        <div class="error">Please check your input — username is required, passwords must match and be at least 8 characters.</div>
        {% endif %}

        <form method="POST" action="/setup">
            <div class="field">
                <label for="username">Username</label>
                <input id="username" type="text" name="username" autofocus autocomplete="username" required>
            </div>
            <div class="field">
                <label for="password">Password</label>
                <input id="password" type="password" name="password" autocomplete="new-password" required>
                <p class="hint">Minimum 8 characters</p>
            </div>
            <div class="field">
                <label for="confirm">Confirm password</label>
                <input id="confirm" type="password" name="confirm" autocomplete="new-password" required>
            </div>
            <button class="btn" type="submit">Create account</button>
        </form>
    </div>
</body>
</html>
